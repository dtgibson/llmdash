# Runtime state — regenerated on each run, kept out of the repo (may contain
# personal usage data, never secrets).
node_modules/
data/
.env
.env.local
*.db
*.db-journal
*.log
.DS_Store

# Local editor/agent tooling (dev-only: preview launch config, git worktrees).
.claude/

# Weft planning docs (pipeline/) are intentionally tracked as the project record.
