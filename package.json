{
  "name": "llmdash",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Personal dashboard for Claude Code usage limits, served locally over Tailscale.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node src/server.js",
    "statusline": "node scripts/statusline.js",
    "test": "node --test"
  }
}
